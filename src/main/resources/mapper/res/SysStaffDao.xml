<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bzh.clod.dao.sunrise.SysStaffDao">

    <select id="findById" parameterType="string" resultType="com.bzh.cloud.entity.sunrise.SysStaff">
        select * from `sys_staff` where staff_id = #{id}
    </select>
    
  
</mapper>
